package fr.gaulupeau.apps.Poche.entity;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import fr.gaulupeau.apps.Poche.entity.QueueItem;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "QUEUE_ITEM".
*/
public class QueueItemDao extends AbstractDao<QueueItem, Long> {

    public static final String TABLENAME = "QUEUE_ITEM";

    /**
     * Properties of entity QueueItem.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Status = new Property(1, int.class, "status", false, "STATUS");
        public final static Property QueueNumber = new Property(2, Long.class, "queueNumber", false, "queue_number");
        public final static Property Action = new Property(3, int.class, "action", false, "ACTION");
        public final static Property ArticleId = new Property(4, Integer.class, "articleId", false, "article_id");
        public final static Property Extra = new Property(5, String.class, "extra", false, "EXTRA");
    };


    public QueueItemDao(DaoConfig config) {
        super(config);
    }
    
    public QueueItemDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"QUEUE_ITEM\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"STATUS\" INTEGER NOT NULL ," + // 1: status
                "\"queue_number\" INTEGER," + // 2: queueNumber
                "\"ACTION\" INTEGER NOT NULL ," + // 3: action
                "\"article_id\" INTEGER," + // 4: articleId
                "\"EXTRA\" TEXT);"); // 5: extra
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"QUEUE_ITEM\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, QueueItem entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getStatus());
 
        Long queueNumber = entity.getQueueNumber();
        if (queueNumber != null) {
            stmt.bindLong(3, queueNumber);
        }
        stmt.bindLong(4, entity.getAction());
 
        Integer articleId = entity.getArticleId();
        if (articleId != null) {
            stmt.bindLong(5, articleId);
        }
 
        String extra = entity.getExtra();
        if (extra != null) {
            stmt.bindString(6, extra);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public QueueItem readEntity(Cursor cursor, int offset) {
        QueueItem entity = new QueueItem( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // status
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // queueNumber
            cursor.getInt(offset + 3), // action
            cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4), // articleId
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // extra
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, QueueItem entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setStatus(cursor.getInt(offset + 1));
        entity.setQueueNumber(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setAction(cursor.getInt(offset + 3));
        entity.setArticleId(cursor.isNull(offset + 4) ? null : cursor.getInt(offset + 4));
        entity.setExtra(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(QueueItem entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(QueueItem entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
